name: A workflow for my Hello World file
on:
  workflow_dispatch:

jobs:
  build:
    name: Hello world action
    runs-on: windows-latest
    steps:
      - name: Print GitHub event action
        run: "Testing" >> $GITHUB_STEP_SUMMARY
      - name: Print GitHub event action
        run: echo $GITHUB_STEP_SUMMARY
